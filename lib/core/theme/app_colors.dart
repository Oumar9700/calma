import 'package:flutter/material.dart';

abstract class AppColors {
  // Primary — Terracotta
  static const Color primary = Color(0xFFC85C3A);
  static const Color primaryLight = Color(0xFFE8856A);
  static const Color primaryDark = Color(0xFF9E3E22);

  // Secondary — Vert mousse
  static const Color secondary = Color(0xFF3D6B4F);
  static const Color secondaryLight = Color(0xFF5A9170);
  static const Color secondaryDark = Color(0xFF284A36);

  // Accent — Or doux
  static const Color accent = Color(0xFFD4A853);
  static const Color accentLight = Color(0xFFE8C47A);

  // Surface — Crème
  static const Color surface = Color(0xFFFAF7F2);
  static const Color surfaceVariant = Color(0xFFF0EAE0);
  static const Color background = Color(0xFFFAF7F2);

  // Text
  static const Color textPrimary = Color(0xFF1C1C1E);
  static const Color textSecondary = Color(0xFF6B6B6B);
  static const Color textTertiary = Color(0xFF9E9E9E);
  static const Color textOnPrimary = Color(0xFFFFFFFF);

  // Semantic
  static const Color success = Color(0xFF3D8B5A);
  static const Color warning = Color(0xFFD4A853);
  static const Color error = Color(0xFFD94F3D);
  static const Color info = Color(0xFF3D6B9E);

  // Borders & Dividers
  static const Color border = Color(0xFFE5DDD4);
  static const Color divider = Color(0xFFEDE8E1);

  // Rating
  static const Color star = Color(0xFFD4A853);

  // Dark mode surfaces
  static const Color darkSurface = Color(0xFF1C1A17);
  static const Color darkBackground = Color(0xFF141210);
  static const Color darkCard = Color(0xFF2A2520);
  static const Color darkBorder = Color(0xFF3D3530);
}
